name: venta_mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+97
environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  sentry_flutter: ^9.26.0
  flutter:
    sdk: flutter
  # Not for our own strings - those are still English only. This is what makes
  # Flutter's *own* strings (date pickers, the text selection menu, everything
  # behind `MaterialLocalizations`) follow the chosen language. Without it,
  # `supportedLocales` outside `en` resolves to nothing and a German-speaking
  # user gets an English calendar in a German app.
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dio: ^5.11.0
  flutter_bloc: ^9.1.1
  equatable: ^2.1.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  hydrated_bloc: ^11.0.0
  go_router: ^17.3.0
  app_links: ^7.2.1
  get_it: ^9.2.1
  flutter_secure_storage: ^10.3.1
  cached_network_image: ^3.4.1
  file_picker: 11.0.2
  image_picker: ^1.2.3
  firebase_core: ^4.12.1
  firebase_messaging: ^16.4.3
  flutter_local_notifications: ^22.2.0
  flutter_callkit_incoming: ^3.1.3
  connectivity_plus: ^7.3.1
  flutter_webrtc: ^1.5.2
  permission_handler: ^12.0.3
  google_fonts: ^8.2.0
  signalr_netcore: ^1.4.4
  path_provider: ^2.1.6
  url_launcher: ^6.3.2
  emoji_picker_flutter: ^4.5.3
  flutter_markdown_plus: ^1.0.12
  markdown: ^7.3.1
  audioplayers: ^6.8.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.4.0
  # MLS (RFC 9420) engine - openmls behind a `dart:ffi` boundary. Local rather
  # than published: it is pinned to the exact openmls build Alpine ships, and
  # the two have to move together or the clients stop being able to join each
  # other's groups.
  venta_mls:
    path: packages/venta_mls
  flutter_blurhash: ^0.9.1
  html: ^0.15.6
  # Decoding, downscaling and re-encoding shelf photos before they go to a model
  # provider. Already present transitively; promoted to a direct dependency
  # because `lib/core/ai/vision_image_prep.dart` imports it, and a transitive
  # dependency can be dropped by an unrelated `pub upgrade`.
  image: ^4.5.4
  # Raw accelerometer, for the full-screen video viewer only. The app itself is
  # portrait-locked, so a phone turned on its side while watching a stream gets
  # no orientation change from the platform - and on a device with rotation lock
  # on, it would get none even if the app were not locked. Reading gravity
  # ourselves is the only way to honour the tilt in both cases.
  sensors_plus: ^7.0.0
  livekit_client: ^2.11.0

dev_dependencies:
  sentry_dart_plugin: ^3.4.0
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  freezed: ^3.2.6-dev.1
  json_serializable: ^6.14.0
  build_runner: ^2.15.1
  bloc_test: ^10.0.0
  mocktail: ^1.0.5
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/sounds/
    # Bundled, not just consumed by flutter_launcher_icons at build time:
    # AppDelegate reads this one at runtime for CallKit's iconTemplateImageData
    # (the app badge on the native call screen). Only its alpha channel is used.
    - assets/icon/app_icon_foreground.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#FAF6EF"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
  min_sdk_android: 21
  # App Store rejects icons with an alpha channel - flatten onto the same
  # cream used for the Android adaptive background instead of the source's
  # transparency.
  remove_alpha_ios: true
  background_color_ios: "#FAF6EF"

sentry:
  upload_debug_symbols: true
  upload_source_maps: true
  project: venta-mobile
  org: alpinebits-klg
